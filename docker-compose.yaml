version: '3.8'
services:
  api-churn-prediction:
    build: ./api
    ports:
      - "8000:8000"
  gradio:
    build:
      context: ./gradio
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
